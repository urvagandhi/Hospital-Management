# MongoDB Configuration
MONGODB_URI=mongodb+srv://23bce078_db_user:cd6nCoyBZa1OCltF@hospital-management.hi7pqgk.mongodb.net/

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRY=24h
REFRESH_TOKEN_SECRET=your_refresh_token_secret_change_this_in_production
REFRESH_TOKEN_EXPIRY=7d

# OTP Configuration
OTP_EXPIRY_MINUTES=5
OTP_LENGTH=6
MAX_OTP_ATTEMPTS=3

# Twilio/SMS Configuration (Placeholder)
SMS_GATEWAY_API_KEY=your_sms_api_key
SMS_GATEWAY_SENDER=YourSender

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=15000
RATE_LIMIT_MAX_REQUESTS=10

# ============================================
# Cloudflare R2 Storage Configuration
# ============================================
# To use cloud storage instead of local storage:
# 1. Go to Cloudflare Dashboard > R2 > Overview
# 2. Create a bucket (e.g., "hospital-files")
# 3. Go to "Manage R2 API Tokens" and create a new token
# 4. Grant "Object Read & Write" permissions
# 5. Copy the credentials below:

R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your-access-key-id-here
R2_SECRET_ACCESS_KEY=your-secret-access-key-here
R2_BUCKET_NAME=hospital-files

# ============================================
# Local File Storage (Development Mode)
# ============================================
# Uncomment to force local storage even if R2 is configured:
# USE_LOCAL_STORAGE=true
# LOCAL_STORAGE_PATH=./uploads
