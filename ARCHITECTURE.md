# System Architecture - Patient Management Module

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                      │
│  ┌──────────┐  ┌──────────────┐  ┌──────────┐  ┌──────────┐ │
│  │Landing  │→ │Patients List │→ │Patient   │→ │FileList  │ │
│  │Page     │  │(Paginated)   │  │Details   │  │(Download)│ │
│  └──────────┘  └──────────────┘  └──────────┘  └──────────┘ │
│         ↓               ↓              ↓              ↓        │
│  ┌───────────────────────────────────────────────────────┐   │
│  │         patientApi.ts (TypeScript Service)           │   │
│  │   9 Functions: fetch, search, download (PDF/ZIP)      │   │
│  └───────────────────────────────────────────────────────┘   │
│         ↓                                                      │
│  ┌───────────────────────────────────────────────────────┐   │
│  │  Components: Button, Toast, ErrorBoundary, Skeleton   │   │
│  └───────────────────────────────────────────────────────┘   │
└──────────────────────┬──────────────────────────────────────┐
                       │ Axios (JWT Token in headers)        │
                       │ REST API Calls                      │
                       ↓                                      │
┌──────────────────────────────────────────────────────────────┐
│                      BACKEND (Node.js)                       │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │               Express Routes (8 endpoints)             │ │
│  │  /api/patients, /:id, /:id/files/:folder              │ │
│  │  /download/pdf, /download/zip (with rate limit)       │ │
│  └─────────────────────────────────────────────────────────┘ │
│         ↓                                                     │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │          patient.controller.js                         │ │
│  │  Handles: CRUD, PDF gen, ZIP gen, validations         │ │
│  └─────────────────────────────────────────────────────────┘ │
│         ↓                      ↓                              │
│  ┌──────────────────────┐  ┌──────────────────────┐          │
│  │ patient.service.js   │  │  r2.service.js       │          │
│  │ Business Logic       │  │ R2 File Operations   │          │
│  │ - CRUD ops           │  │ - upload, download   │          │
│  │ - Validations        │  │ - delete, stream     │          │
│  │ - Auto-delete (90d)  │  │ - signed URLs        │          │
│  └──────────────────────┘  └──────────────────────┘          │
│         ↓                      ↓                              │
│  ┌──────────────────────┐  ┌──────────────────────┐          │
│  │ MongoDB              │  │ Cloudflare R2 (S3)   │          │
│  │ - Patient docs       │  │ - Medical files      │          │
│  │ - Folders/files meta │  │ - PDFs, images, etc  │          │
│  │ - TTL indexes        │  │ - Secure URLs        │          │
│  └──────────────────────┘  └──────────────────────┘          │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  autoDelete.job.js (Node-cron)                         │ │
│  │  Runs: Daily 2 AM UTC                                  │ │
│  │  Action: Delete patients > 90 days from DB + R2        │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    USER INTERACTION                         │
└─────────────────┬───────────────────────────────────────────┘
                  │ 1. Click "View Patients"
                  ↓
         ┌────────────────────┐
         │ PatientsList Page   │
         │ fetchPatients()     │ 2. API Call
         └────────┬───────────┘
                  │ 3. GET /api/patients (paginated)
                  ↓
         ┌────────────────────────────┐
         │ patient.controller         │
         │ .getPatients()             │
         └────────┬───────────────────┘
                  │ 4. Query DB
                  ↓
         ┌────────────────────────────┐
         │ patient.service.js         │
         │ .getPatients(hospitalId)   │
         └────────┬───────────────────┘
                  │ 5. MongoDB query
                  ↓
         ┌────────────────────────────┐
         │ Patient Collection         │
         │ filtered by hospitalId     │
         └────────┬───────────────────┘
                  │ 6. Returns patients (no file URLs for security)
                  ↓
         ┌────────────────────────────┐
         │ Browser                    │
         │ Display list of patients   │
         └────────┬───────────────────┘
                  │ 7. User clicks patient
                  ↓
         ┌────────────────────────────┐
         │ PatientDetails Page        │
         │ fetchPatientById()         │ 8. API Call
         └────────┬───────────────────┘
                  │ 9. GET /api/patients/:id
                  ↓
         ┌────────────────────────────┐
         │ MongoDB returns patient    │
         │ with folders + files meta  │
         └────────┬───────────────────┘
                  │ 10. Display folders
                  ↓
         ┌────────────────────────────┐
         │ User clicks folder         │
         │ → FileList Page            │ 11. Navigate to FileList
         └────────┬───────────────────┘
                  │ 12. fetchFolderFiles()
                  ↓
         ┌────────────────────────────┐
         │ GET /api/patients/:id/files/:folder
         │ Returns file listings      │
         └────────┬───────────────────┘
                  │ 13. Display files
                  ↓
         ┌────────────────────────────┐
         │ User clicks Download (PDF) │
         │ downloadFolderPdf()        │ 14. Download trigger
         └────────┬───────────────────┘
                  │ 15. GET /api/patients/:id/folders/:folder/pdf
                  ↓
         ┌────────────────────────────┐
         │ patient.controller         │
         │ .downloadFolderPdf()       │
         │ - Uses pdfkit              │
         │ - Generates PDF in-stream  │ 16. Generate & stream
         │ - Pipes to response        │
         └────────┬───────────────────┘
                  │ 17. Browser receives PDF blob
                  ↓
         ┌────────────────────────────┐
         │ patientApi.ts              │
         │ .downloadBlob(blob, name)  │ 18. Trigger download
         └────────┬───────────────────┘
                  │ 19. File saved to Downloads/
                  ↓
         ┌────────────────────────────┐
         │ Success Toast shown        │
         └────────────────────────────┘
```

## 🔄 Auto-Delete Cron Flow

```
┌─────────────────────────────────────────────────┐
│ Server Starts (backend/src/index.js)            │
└────────────────────┬────────────────────────────┘
                     │
                     ↓
        ┌────────────────────────────┐
        │ scheduleAutoDelete()        │
        │ Called from index.js        │
        └────────────────┬───────────┘
                         │
                         ↓
        ┌────────────────────────────────────┐
        │ cron.schedule("0 2 * * *")         │
        │ Runs daily at 2:00 AM UTC         │
        └────────────────┬───────────────────┘
                         │
              (Waits 23h 59m 59s...)
                         │
                         ↓
        ┌────────────────────────────────────────┐
        │ patientService.deleteOldPatients(90)   │
        │ Executes every 24 hours               │
        └────────────────┬──────────────────────┘
                         │
        ┌────────────────┴──────────────────┐
        │                                   │
        ↓                                   ↓
┌──────────────────────────┐      ┌──────────────────┐
│ MongoDB Query            │      │                  │
│ createdAt < 90 days ago  │      │                  │
│ Returns: [patients]      │      │                  │
└──────────────┬───────────┘      │                  │
               │                  │                  │
               ↓                  ↓                  │
        ┌──────────────────────────────┐            │
        │ For each patient:            │            │
        │                              │            │
        │ 1. r2.service.deleteFolder() │            │
        │    Delete all files from R2  │            │
        │    using patient-specific    │            │
        │    folder prefix             │            │
        │                              │            │
        │ 2. Patient.deleteOne({_id})  │            │
        │    Remove from MongoDB       │            │
        │                              │            │
        └──────────┬───────────────────┘            │
                   │                                │
                   ↓                                │
        ┌──────────────────────────────┐            │
        │ Log Results:                 │            │
        │ "Deleted X patients and      │            │
        │  Y files from R2"            │────────────┘
        │                              │
        │ Next execution: 24h later    │
        │ (2:00 AM UTC tomorrow)       │
        └──────────────────────────────┘
```

## 🔒 Security Flow

```
┌─────────────────────────────────────────────────────────┐
│ User Request with JWT Token                            │
└────────────────────┬────────────────────────────────────┘
                     │ Authorization: Bearer eyJxx...
                     ↓
        ┌────────────────────────────────────┐
        │ Express Middleware                  │
        │ verifyAccessToken()                │
        │ - Extract token from header        │
        │ - Verify signature with secret     │
        │ - Decode to get hospitalId + user  │
        └────────────────┬───────────────────┘
                         │
                    ✅ Valid? →──┐
                         │       │
                    ❌ Invalid?  ↓
                         │   Return 401
                         │
                         ↓
        ┌────────────────────────────────────┐
        │ Controller receives request        │
        │ + hospitalId from token            │
        └────────────────┬───────────────────┘
                         │
                         ↓
        ┌────────────────────────────────────┐
        │ patient.service.getPatients()      │
        │ Query: Patient.find({              │
        │   hospitalId: decoded.hospitalId   │
        │ })                                 │
        │                                    │
        │ → Only patient's own hospital data │
        │   No cross-hospital access         │
        └────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Download Endpoint Rate Limiting                        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
        ┌────────────────────────────────────┐
        │ GET /api/patients/:id/download/zip │
        │ Applied: patientLimiter middleware │
        │ Settings: 10 requests/minute/IP    │
        └────────────────┬───────────────────┘
                         │
               Counter++ ↓
                         │
        Limit Exceeded?
        ├─ NO → Process download
        └─ YES → Return 429 (Too Many Requests)
```

## 📱 Component Hierarchy

```
┌─ App
│   └─ AppRoutes
│       └─ Router
│           └─ AuthProvider
│               └─ ErrorBoundary
│                   ├─ Route: /login
│                   │   └─ Login Page
│                   │
│                   ├─ Route: /verify-otp
│                   │   └─ OtpVerification Page
│                   │
│                   ├─ ProtectedRoute: /
│                   │   └─ LandingPage
│                   │       └─ Button, useAuth hook
│                   │
│                   ├─ ProtectedRoute: /patients
│                   │   └─ PatientsList
│                   │       ├─ SkeletonLoader (while loading)
│                   │       ├─ Table (patients)
│                   │       ├─ Toast (errors)
│                   │       └─ Button (pagination)
│                   │
│                   ├─ ProtectedRoute: /patients/:patientId
│                   │   └─ PatientDetails
│                   │       ├─ Patient Info Card
│                   │       ├─ Folder Grid
│                   │       │   └─ Folder Cards (clickable)
│                   │       └─ Toast (errors)
│                   │
│                   └─ ProtectedRoute: /patients/:patientId/files/:folder
│                       └─ FileList
│                           ├─ Folder Info Card
│                           ├─ Download Options
│                           │   ├─ Button (Folder PDF)
│                           │   ├─ Button (Folder ZIP)
│                           │   ├─ Button (All PDF)
│                           │   └─ Button (All ZIP)
│                           ├─ Files Table
│                           └─ Toast (feedback)
```

## 🎯 Request/Response Cycle

```
REQUEST:
┌──────────────────────────────────────┐
│ GET /api/patients?limit=20&skip=0    │
│ Headers:                             │
│   Authorization: Bearer JWT_TOKEN    │
│   Content-Type: application/json     │
└──────────────────────────────────────┘
           ↓
┌──────────────────────────────────────┐
│ Express receives & parses            │
│ Middleware: verifyAccessToken()      │
│ Extracts: hospitalId from JWT        │
└──────────────────────────────────────┘
           ↓
┌──────────────────────────────────────┐
│ Controller: getPatients()            │
│ - Validates request params           │
│ - Calls service layer                │
└──────────────────────────────────────┘
           ↓
┌──────────────────────────────────────┐
│ Service: getPatients(hospitalId)     │
│ - Builds query: {hospitalId}         │
│ - Excludes fileUrls (security)       │
│ - Paginates results                  │
└──────────────────────────────────────┘
           ↓
┌──────────────────────────────────────┐
│ MongoDB returns 20 patients + count  │
└──────────────────────────────────────┘
           ↓
RESPONSE:
┌──────────────────────────────────────┐
│ 200 OK                               │
│ Content-Type: application/json       │
│ Body: {                              │
│   "patients": [{                     │
│     "_id": "123",                    │
│     "patientName": "John Doe",       │
│     "email": "john@hospital.com",    │
│     "folders": [...]                 │
│   }],                                │
│   "total": 147                       │
│ }                                    │
└──────────────────────────────────────┘
           ↓
Frontend receives & parses JSON
           ↓
Update React state
           ↓
Component re-renders with patients list
```

## 📊 Database Schema Relationship

```
Hospital
  │
  ├─ _id (ObjectId)
  └─ hospitalName, admin, etc.

Patient (with hospitalId reference)
  │
  ├─ _id (ObjectId)
  ├─ hospitalId → Hospital._id (indexed for isolation)
  ├─ patientName (String)
  ├─ medicalRecordNumber (Unique per hospital)
  ├─ email, phone, dateOfBirth
  ├─ folders: [Folder]
  │   ├─ name (String)
  │   ├─ files: [File]
  │   │   ├─ fileName (String)
  │   │   ├─ fileUrl (String, presigned R2 URL)
  │   │   ├─ size (Number, bytes)
  │   │   ├─ mimeType (String, e.g., "application/pdf")
  │   │   └─ uploadedAt (Date)
  │   └─ createdAt (Date)
  ├─ createdAt (Date, indexed for 90-day deletion)
  ├─ updatedAt (Date)
  └─ TTL Index: { createdAt: 1 } for automatic cleanup

Indexes:
  - { hospitalId: 1 } - Fast hospital isolation
  - { createdAt: 1 } - Fast 90-day queries
  - { hospitalId: 1, createdAt: 1 } - Optimized for both
  - { medicalRecordNumber: 1 } - Unique per hospital
```

## 🚀 Deployment Architecture

```
Production Environment:
┌──────────────────────────────────────────────────────┐
│ CloudFlare CDN                                       │
│ (Static files, caching)                              │
└────────────────┬─────────────────────────────────────┘
                 │
    ┌────────────┴────────────┐
    ↓                         ↓
┌─────────────┐         ┌────────────┐
│ Frontend    │         │ Backend    │
│ (React SPA) │         │ (Node.js)  │
│ localhost   │         │ port 5000  │
│ :3000       │         │            │
└─────────────┘         └─────┬──────┘
                               │
                ┌──────────────┼──────────────┐
                ↓              ↓              ↓
         ┌──────────┐   ┌──────────┐   ┌──────────┐
         │MongoDB   │   │Cloudflare│   │Node-cron │
         │Database  │   │R2 (S3)   │   │Jobs      │
         │localhost │   │Storage   │   │          │
         │:27017    │   │          │   │2 AM UTC  │
         └──────────┘   └──────────┘   └──────────┘

SSL/TLS:
  Frontend ←→ CloudFlare (HTTPS)
  CloudFlare ←→ Backend (HTTPS)
  Backend ←→ MongoDB (Secure)
  Backend ←→ R2 (HTTPS)
```

---

This architecture ensures:

- ✅ **Scalability** - Separated frontend/backend, horizontal scaling ready
- ✅ **Security** - Hospital isolation, JWT auth, rate limiting
- ✅ **Performance** - Streaming, pagination, indexed queries
- ✅ **Reliability** - Error handling, auto-delete, logging
- ✅ **Maintainability** - Clear separation of concerns, documented
