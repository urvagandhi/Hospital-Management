<?xml version="1.0" encoding="utf-8"?>
<!--
     Opaque, rounded background for overflow / popup menus. Uses the solid
     elevated-surface colour (light + dark resolved via values/values-night)
     so menu items don't bleed through to the activity behind them.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/color_surface_elevated" />
    <corners android:radius="@dimen/radius_md" />
    <stroke
        android:width="1dp"
        android:color="@color/color_glass_stroke" />
</shape>
