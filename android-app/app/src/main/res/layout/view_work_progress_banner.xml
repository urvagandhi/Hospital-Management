<?xml version="1.0" encoding="utf-8"?>
<!-- Self-contained banner that surfaces in-flight WorkManager jobs (downloads,
     uploads, sync) at the top of the Dashboard. Auto-hides when no work is in
     RUNNING/ENQUEUED state. Bound by WorkProgressBanner.kt. -->
<com.hospital.management.ui.components.WorkProgressBanner xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/workProgressBanner"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="8dp"
    android:visibility="gone"
    app:cardBackgroundColor="@color/brand_primary"
    app:cardCornerRadius="14dp"
    app:cardElevation="6dp"
    app:strokeColor="@color/brand_primary"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="14dp">

        <ProgressBar
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_marginEnd="14dp"
            android:indeterminate="true"
            android:indeterminateTint="@android:color/white" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/bannerTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/inter_semibold"
                android:textSize="14sp"
                android:textColor="@android:color/white"
                tools:text="Background activity" />

            <TextView
                android:id="@+id/bannerSubtext"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/inter"
                android:textSize="12sp"
                android:textColor="#E6FFFFFF"
                tools:text="2 uploading · 1 syncing" />
        </LinearLayout>
    </LinearLayout>

</com.hospital.management.ui.components.WorkProgressBanner>
