<?xml version="1.0" encoding="utf-8"?>
<!-- Self-contained banner that surfaces in-flight WorkManager jobs (downloads,
     uploads, sync) at the top of the Dashboard. Auto-hides when no work is in
     RUNNING/ENQUEUED state. Bound by WorkProgressBanner.kt. -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/workProgressBanner"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="8dp"
    android:visibility="gone"
    app:cardBackgroundColor="@color/color_background"
    app:cardCornerRadius="14dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp">

        <ProgressBar
            android:id="@+id/bannerSpinner"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:indeterminate="true"
            android:layout_marginEnd="12dp" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/bannerTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/inter_semibold"
                android:textSize="13sp"
                android:textColor="@color/color_on_surface"
                tools:text="Background activity" />

            <TextView
                android:id="@+id/bannerSubtext"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/inter"
                android:textSize="12sp"
                android:textColor="@color/color_on_surface_variant"
                tools:text="2 uploading · 1 syncing" />
        </LinearLayout>
    </LinearLayout>

</androidx.cardview.widget.CardView>
