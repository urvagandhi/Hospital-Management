<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Disable cleartext traffic globally -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Production API domain — inherits base-config (system CAs, no cleartext) -->
    <!-- Certificate pins removed: Render.com rotates certs via Google Trust Services;
         pinning causes SSLPeerUnverifiedException after every rotation. System CA
         trust (enforced by base-config above) is sufficient here. -->
    <domain-config>
        <domain includeSubdomains="true">hospital-management-8lbf.onrender.com</domain>
        <domain includeSubdomains="true">mymedivault.in</domain>
    </domain-config>

    <!-- Allow localhost cleartext for development only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
