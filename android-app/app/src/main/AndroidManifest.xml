<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:name=".HospitalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.HospitalManagement"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.auth.OtpActivity" />
        <activity android:name=".ui.dashboard.DashboardActivity" />
        <activity android:name=".ui.admission.AdmissionActivity" />
        <activity android:name=".ui.scanner.ScannerActivity" />
        <activity android:name=".ui.upload.UploadActivity" />
        <activity android:name=".ui.patients.PatientListActivity" />
        <activity android:name=".ui.patients.PatientDetailsActivity" />
        <activity android:name=".FolderViewActivity" />
        <activity android:name=".FolderDetailsActivity" />
        <activity android:name=".ScannerActivity" />
        
    </application>

</manifest>
